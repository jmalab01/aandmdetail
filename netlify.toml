[build]
  publish = "."
  command = "echo 'Static site - no build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Disable Jekyll
[build.environment]
  JEKYLL_ENV = "production"
